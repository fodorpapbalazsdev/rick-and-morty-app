import { CharacterStatusDirectiveDirective } from './character-status-directive.directive';

describe('CharacterStatusDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CharacterStatusDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
